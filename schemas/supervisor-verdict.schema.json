{
  "title": "Galley Supervisor Verdict",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "status",
    "summary",
    "acceptance_passes",
    "quality_passes",
    "findings",
    "discussion_items"
  ],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["accepted", "needs_revision", "needs_supervisor_review", "hard_stop"]
    },
    "summary": {
      "type": "string",
      "minLength": 1
    },
    "acceptance_passes": {
      "type": "array",
      "description": "The current set of passed task AC and revision:<request.id> IDs.",
      "items": { "type": "string", "minLength": 1 }
    },
    "quality_passes": {
      "type": "array",
      "description": "The current set of passed configured quality-dimension IDs.",
      "items": { "type": "string", "minLength": 1 }
    },
    "findings": {
      "type": "array",
      "description": "The complete current repair batch. Each string states the concrete failure, required outcome, affected location, and closure verification.",
      "items": { "type": "string", "minLength": 1 }
    },
    "discussion_items": {
      "type": "array",
      "description": "Information to show the human reviewer in the pull request.",
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
