interface:
  display_name: "Galley"
  short_description: "Use for Galley tasks, profiles, daemon, evidence, and troubleshooting"
  default_prompt: "Use $galley for this request. Select the matching flow from SKILL.md and load only its required references."

policy:
  allow_implicit_invocation: true
